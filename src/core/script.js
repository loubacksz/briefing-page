//const submit = document.getElementById("submit");
//const camposDoFormulario = document.querySelectorAll("[required]");

/*camposDoFormulario.forEach((campo) => {
    campo.addEventListener('blur', () => verificaCampo(campo));
})*/

// o que verificar no campo? o que pode dar errado? como posso me defender?
// CPFs não podem ter todos os digitos iguais
// Remover caracteres não numéricos, se aplicável
// Verificar se tem 11 digitos
// Calculo de verificação
// Verificar se o CPF é duplicado
/*document.getElementById('linkBriefing').addEventListener('click', function(event){
    event.preventDefault();
    window.location = '../static/briefing-page.html';
    verificaCampo();
});*/
function verificaCampo() {     
    console.log(window.location);
}